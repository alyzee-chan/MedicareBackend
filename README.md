# MedicareBackend

Spring Boot backend for MediCare+ with SQLite storage.

## Run

```bash
./gradlew bootRun
```

On Windows:

```bash
gradlew.bat bootRun
```

## Database

The app uses a local `medicare.db` SQLite file created automatically at startup.
